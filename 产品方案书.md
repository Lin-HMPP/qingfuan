# 青付安（QingFuAn）产品方案书

> **青年预付消费管理 PWA**  
> 版本：V2.0 | 更新日期：2026-08-10 | 阶段：收尾交付

---

## 目录

1. [产品概述](#一产品概述)
2. [目标用户与场景](#二目标用户与场景)
3. [核心价值主张](#三核心价值主张)
4. [功能架构](#四功能架构)
5. [用户流程](#五用户流程)
6. [技术架构](#六技术架构)
7. [数据模型](#七数据模型)
8. [风险规则引擎](#八风险规则引擎)
9. [UI/UX 设计体系](#九uiux-设计体系)
10. [安全与隐私](#十安全与隐私)
11. [部署与运维](#十一部署与运维)
12. [未来规划](#十二未来规划)

---

## 一、产品概述

### 1.1 产品定义

**青付安**是一款面向大学生和职场新人的**预付消费全生命周期管理 PWA**。它帮助用户在办理健身卡、培训课、摄影套餐等预付消费时——**买前测算风险、买后管理资产、纠纷一键取证**。

### 1.2 产品定位

| 维度 | 说明 |
|------|------|
| **形态** | PWA（Progressive Web App），免安装，浏览器即用 |
| **平台** | 移动端优先，适配微信内置浏览器 |
| **技术** | 纯前端，localStorage 全量本地存储，零服务端 |
| **核心场景** | 健身/舞蹈、培训课程、摄影套餐、美容美发（可自定义扩展） |
| **竞品替代** | 替代 Excel 记账、备忘录截图、微信收藏夹的碎片化管理 |

### 1.3 解决的核心问题

| 痛点 | 青付安的解法 |
|------|-------------|
| 办卡前不知道划不划算 | 套餐录入 → 17 条规则引擎 → 决策卡 + 风险报告 |
| 不知道什么时候该用完 | 有效期-频率智能匹配 + 到期预警 |
| 核销记录散落各处 | 统一核销打卡，实时更新剩余权益 |
| 门店跑路维权难 | 证据资料夹归集四类凭证，一键导出维权报告 |
| 敏感财务数据怕泄露 | 全量本地存储 + PIN 码锁定 |

---

## 二、目标用户与场景

### 2.1 用户画像

| 画像 | 特征 | 典型场景 |
|------|------|---------|
| **大学生** | 预算有限、首次接触预付消费、防坑意识弱 | 考研培训班、健身房学期卡 |
| **职场新人**（毕业1-3年） | 有稳定收入、消费升级、时间碎片化 | 健身年卡、美容美发充值、摄影写真 |
| **理性消费者** | 注重性价比、习惯比价、需要决策依据 | 多个套餐方案对比评估 |

### 2.2 四类核心消费场景

应用内置 **4 种预设场景 + 自定义场景**，每种场景拥有专属交互文案：

| 场景 | 典型客单价 | 专属术语 | 特殊风险 |
|------|-----------|---------|---------|
| **健身/舞蹈** | ¥980 ~ ¥5,000 | 次卡/年卡/私教课/请假 | 门店跑路高发 |
| **培训课程** | ¥2,000 ~ ¥30,000 | 课时/全程班/休学/转让 | 名师离职/课程缩水 |
| **摄影套餐** | ¥999 ~ ¥10,000 | 套数/精修张/约拍/延期 | 隐性消费/成片质量争议 |
| **美容美发** | ¥500 ~ ¥5,000 | 次卡/充卡/护理/暂停 | 充值频繁/消费频率不稳定 |

---

## 三、核心价值主张

### 3.1 产品 Slogan

> **预付消费，看得懂、算得清、管得住**

### 3.2 三大核心能力

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   看得懂      │    │   算得清      │    │   管得住      │
│              │    │              │    │              │
│ 决策卡可视化  │    │ 规则引擎自动  │    │ 全周期资产   │
│ 结论先行     │    │ 17条风险分析  │    │ 跟踪管理     │
│ 说人话       │    │ 成本测算      │    │ 证据一键导出 │
│ 行动清单     │    │ 双情景对比    │    │ PIN安全锁    │
└──────────────┘    └──────────────┘    └──────────────┘
```

### 3.3 差异化亮点

1. **场景化文案**：不同消费场景使用不同的行业术语和 placeholder，降低用户理解成本
2. **两种录入模式**：快速录入（10 秒建卡）vs 套餐录入（含 17 项风险评估），匹配不同使用场景
3. **无限次模式**：专为充卡/年卡设计的打卡追踪 + 回本分析，突破传统次卡的思维框架
4. **证据资料夹**：首创 8 类材料清单 + 缺失可点击直传 + HTML 维权报告一键导出
5. **零隐私顾虑**：100% 本地存储，所有数据不经过任何服务器

---

## 四、功能架构

### 4.1 功能模块总览

```
青付安 PWA
├── 🏠 首页（/home）
│   ├── 即将到期提醒（30天内）
│   ├── 双卡片入口（购买前检查 / 我的资产）
│   ├── 资产总览（总额 + 卡数）
│   ├── 证据资料夹入口
│   ├── 场景快捷入口（4个常用 + 自定义）
│   └── ⊕ 快速录入弹窗
│
├── ⚡ 快速录入（/quick-input）
│   ├── 消费场景选择（4+自定义）
│   ├── 核心5字段：门店 + 总价 + 类型(次卡/充卡) + 次数 + 期限
│   └── 一键创建资产卡 + 自动创建资料夹
│
├── 📝 套餐录入（/package-input）
│   ├── 5 模块分步填写 + 折叠状态进度条
│   │   ├── 模块一：套餐基础费用（自动成本预览）
│   │   ├── 模块二：个人使用规划（频率智能匹配）
│   │   ├── 模块三：签约&收款主体（5种支付渠道）
│   │   ├── 模块四：履约规则选择（退款/转卡/暂停）
│   │   └── 模块五：促销附加说明（选填）
│   ├── 8 类材料上传（拍摄/相册/文件）
│   ├── 场景化文案（SCENE_COPY）动态切换
│   ├── 每10秒自动保存草稿
│   └── 确认 → 决策卡
│
├── 📊 决策卡（/decision-card）
│   ├── 结论横幅（绿/橙/红三档 + SVG 图标）
│   ├── 套餐速览（次数 / 总价 / 有效期 / 单价）
│   ├── 花费算账
│   │   ├── 单次/日均成本 + 计算公式
│   │   ├── 月预算对比（超预算红色预警）
│   │   ├── 消耗节奏分析 + 进度条
│   │   └── 回本参考（无限次专有）
│   ├── 需要关注的问题（高/中风险分类）
│   ├── 已通过检查（可折叠）
│   ├── 付款前行动清单（去重后的高危建议）
│   ├── 内嵌六维度详情（星级评分 + 各规则明细）
│   └── 确认生成资产 → 自动创建资料夹 + 导入图片
│
├── 📋 风险报告（/risk-report）
│   ├── 套餐摘要卡片
│   ├── 总体风险等级（色标）
│   ├── 六维度逐板块展示（含评分/命中规则/行动建议）
│   ├── 深度成本测算（乐观 vs 悲观双情景）
│   │   或 充卡价值分析（无限次：日/月均成本 + 回本分析）
│   └── 到期使用进度预估
│
├── 💳 资产列表（/asset-list）
│   ├── 总资产统计栏（总额 / 卡数 / 即将到期数）
│   ├── 资产卡片（门店·场景 / 进度 / 单次成本 / 新卡高亮）
│   ├── 快捷操作：核销 / 凭证入口
│   ├── 管理模式：编辑弹窗 / 级联删除
│   └── 新增预付卡入口
│
├── 🏷️ 持仓卡（/asset-detail）
│   ├── 资产数据面板
│   │   ├── 普通模式：剩余次数/单次成本/剩余价值/使用进度条
│   │   └── 无限次模式：累计到店/日均成本/单次到店成本/时间进度条
│   ├── 资产缩水预警（剩余≤30天 + 使用率偏低）
│   ├── 暂停自动恢复检测
│   └── 四大功能：核销 / 暂停改期 / 申请退款 / 查看凭证
│
├── ✓ 核销录入（/write-off）
│   ├── 普通模式：手动输入消耗次数（防未来日期/超限）
│   ├── 无限次模式：自动计1次打卡 + 频率建议
│   ├── 场景化文案（单位/标签动态切换）
│   ├── 暂停/过期自动拦截
│   └── 历史记录列表（可编辑/删除）
│
├── 📁 证据资料夹（/evidence-folder）
│   ├── 全部文件夹列表
│   ├── 材料完整性检查清单（8类）
│   │   └── 缺失项可点击直达上传
│   ├── 文件管理（上传/查看/删除/材料管理弹窗）
│   ├── 单个导出 + 一键打包 → HTML 维权报告
│   ├── 资料夹编辑（名称/绑定资产）
│   └── 级联删除（文件夹 → 文件）
│
├── 📂 新建资料夹（/folder-create）
│   └── 绑定资产 + 命名
│
└── 👤 我的（/mine）
    ├── 用户卡片 + PIN 锁定/解锁
    ├── 三栏统计（总金额 / 卡数 / 即将到期）
    ├── 菜单：
    │   ├── 使用指南（5步指引弹窗）
    │   ├── 规则说明（17条规则详解弹窗）
    │   ├── 隐私设置
    │   ├── 本地凭证管理
    │   └── 重置所有数据
    └── PIN 锁：设置 + 验证 + 忘记重置
```

### 4.2 公共组件（11个）

| 组件 | 功能 |
|------|------|
| **tab-bar** | 底部导航栏（首页/资产/证据/我的）内联 SVG 响应式变色 |
| **toast** | 全局深色 Toast，3 秒自动关闭，点击关闭，DOM 兜底 |
| **pin-lock** | PIN 数字键盘，设置/验证模式 |
| **scene-picker** | 场景选择器（4 种 + 自定义入口） |
| **image-picker** | 图片采集（拍摄/相册/文件 → base64 多选） |
| **refund-checklist** | 退款材料核对清单（支持无限次时间比例模式） |
| **pause-transfer** | 暂停锁卡 + 编辑套餐有效期 |
| **asset-confirm** | 确认生成资产弹窗 |
| **exit-confirm** | 退出确认弹窗 |
| **write-off-detail** | 核销详情弹窗（编辑/删除） |
| **package-loading** | 打包导出加载动画 |

---

## 五、用户流程

### 5.1 核心用户旅程

```
                    ┌─→ 快速录入（10秒）──┐
                    │                    ↓
  首页 ──→ 选择场景 ─┤              创建资产卡 ──→ 资产列表
                    │                    ↑
                    └─→ 套餐录入（5模块）─┤
                         │               │
                         ↓               │
                       决策卡 ──→ 确认生成┘
                         │
                         ↓（可选）
                       风险报告
                         │
                         ↓
                    确认生成资产

  资产列表 ──→ 持仓卡 ──→ 核销打卡
              │        │
              │        ├──→ 暂停/改期
              │        ├──→ 申请退款参考
              │        └──→ 查看证据资料夹
              │
              └──→ 编辑/删除资产（级联清理）
```

### 5.2 关键交互约定

| 约定 | 说明 |
|------|------|
| **导航方式** | 统一 `router.push('/path')`，禁止 `router.back()`（防止浏览器退出弹窗） |
| **页面间传参** | 套餐录入 → 决策卡通过 `sessionStorage`（`qf_package_data`），图片通过 `localStorage`（`qf_package_images`） |
| **返回恢复** | 决策卡 → 套餐录入通过 `sessionStorage`（`qf_draft_back`），自动回填所有字段 |
| **Toast** | 全局使用 `window.__toast('消息')`，带 DOM 兜底 |
| **锁保护** | 大部分操作入口通过 `guard()` 函数检查锁状态，锁定后提示解锁 |

### 5.3 数据流转图

```
localStorage（9 类数据）
    │
    ├── qf_account     ← 账户信息（预留）
    ├── qf_assets      ← 资产卡片（增删改查）
    ├── qf_writeoffs   ← 核销记录（按资产ID筛选）
    ├── qf_pauses      ← 暂停/改期记录
    ├── qf_folders     ← 资料夹（关联资产ID）
    ├── qf_files       ← 凭证文件（base64，关联文件夹ID）
    ├── qf_draft       ← 套餐录入草稿（7天过期）
    ├── qf_logs        ← 日志（预留）
    ├── qf_package_images ← 录入页上传图片临时存储
    ├── qf_pin_hash    ← PIN 哈希（btoa 算法）
    └── qf_unlocked    ← 解锁状态标记
```

---

## 六、技术架构

### 6.1 技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| **框架** | Vue 3 (Composition API / `<script setup>`) | 现代化响应式框架 |
| **构建** | Vite 5 | 极速 HMR，开发端口 3000 |
| **路由** | Vue Router 4 (Hash 模式) | 兼容 GitHub Pages 部署 |
| **状态管理** | Pinia (lock store) | 全局锁状态共享 |
| **样式** | SCSS | 全局变量注入（vite.config.js） |
| **存储** | localStorage + sessionStorage | 纯前端，零后端 |
| **图标** | 内联 SVG（30+ 图标库） | 24×24 viewBox，颜色 CSS 继承 |
| **分析** | 百度统计 + Microsoft Clarity | 用户行为埋点 |
| **部署** | GitHub Pages | push main → GitHub Actions 自动构建 |
| **PWA** | manifest.json + Service Worker | 可添加至主屏幕 |

### 6.2 项目结构

```
qingfuan/
├── App.vue                 # 全局样式 + PIN锁 + Toast + 锁横幅
├── main.js                 # 路由配置（11个路由）
├── index.html              # 入口
├── vite.config.js          # @别名 + SCSS变量注入
├── common/                 # 公共逻辑层
│   ├── storage.js          # localStorage CRUD（9类数据 + KEYS常量）
│   ├── rules-engine.js     # 17条规则引擎 + 六维度评分 + 成本测算
│   ├── validator.js        # 表单校验工具
│   ├── analytics.js        # 埋点（百度统计 + Clarity）
│   └── icons.js            # 30+ SVG 图标库
├── store/
│   └── lock.js             # 全局锁状态（locked / showLockBanner）
├── pages/                  # 11个页面
│   ├── index/              # 首页
│   ├── quick-input/        # 快速录入
│   ├── package-input/      # 套餐录入（SCENE_COPY 场景化文案）
│   ├── decision-card/      # 决策卡
│   ├── risk-report/        # 风险报告
│   ├── asset-list/         # 资产列表
│   ├── asset-detail/       # 持仓卡
│   ├── write-off/          # 核销录入
│   ├── evidence-folder/    # 证据资料夹
│   ├── folder-create/      # 新建资料夹
│   └── mine/               # 我的
├── components/             # 11个组件
│   ├── tab-bar/            # 底部导航
│   ├── toast/              # 深色Toast
│   ├── pin-lock/           # PIN锁数字键盘
│   ├── scene-picker/       # 场景选择
│   ├── image-picker/       # 图片采集
│   ├── refund-checklist/   # 退款清单
│   ├── pause-transfer/     # 暂停锁卡
│   ├── asset-confirm/      # 确认生成
│   ├── exit-confirm/       # 退出确认
│   ├── write-off-detail/   # 核销详情
│   └── package-loading/    # 打包加载动画
└── static/icons/           # 8个Tab SVG图标
```

### 6.3 性能策略

- **路由懒加载**：11 个页面组件全部动态 import
- **CSS 作用域隔离**：所有页面/组件使用 `<style scoped>`
- **base64 图片优化**：限制上传文件通过 FileReader 转 base64 存储
- **草稿自动清理**：7 天未编辑的草稿自动清除
- **图片分流存储**：套餐录入图片存 localStorage，sessionStorage 仅存元数据

---

## 七、数据模型

### 7.1 资产（Asset）

```javascript
{
  id: 'ast_' + timestamp,       // 唯一标识
  scene: '健身/舞蹈',            // 消费场景
  name: 'XX健身·套餐',           // 资产名称
  storeName: 'XX健身工作室',     // 门店名称
  contractName: '',              // 合同签约主体
  payeeName: '',                 // 收款账户名
  totalPrice: 2880,              // 预付总价（元）
  totalTimes: 96,                // 总次数（无限次模式 = 999）
  usedTimes: 0,                  // 已用次数
  giftTimes: 0,                  // 赠送次数
  validityMonths: 12,            // 有效期（月）
  weeklyFreq: 3,                 // 计划周频率
  monthlyBudget: 500,            // 月度预算
  unlimited: false,              // 是否无限次模式
  noExpiry: false,               // 是否无固定期限
  refundRule: '',                // 退款规则
  transferRule: '',              // 转卡规则
  pauseRule: '',                 // 暂停规则
  groupBuyPlatform: '',          // 团购平台（meituan/dianping/douyin/other）
  status: 'active',              // active / paused / expired
  createdAt: '2026-08-10T...',   // 创建时间
  updatedAt: '2026-08-10T...'    // 更新时间
}
```

### 7.2 核销记录（WriteOff）

```javascript
{
  id: 'wo_' + timestamp,
  assetId: 'ast_...',
  date: '2026-08-10',
  hours: 2,                      // 消耗次数（无限次模式 = 1）
  note: '腿部训练',
  remainingAfter: 94,            // 剩余次数/累计次数
  createdAt: '2026-08-10T...'
}
```

### 7.3 资料夹（Folder）

```javascript
{
  id: 'fld_' + timestamp,
  assetId: 'ast_...',            // 绑定的资产ID
  name: 'XX健身 · 健身/舞蹈 凭证',
  note: '',
  createdAt: '2026-08-10T...'
}
```

### 7.4 凭证文件（File）

```javascript
{
  id: 'fil_' + timestamp,
  folderId: 'fld_...',
  name: '合同照片.jpg',
  type: 'image',                 // image / pdf / ...（扩展名）
  size: '245.3KB',
  materialType: 'contract',      // contract/payment/poster/chat/writeoff/notice/refund_chat/negotiation
  dataUrl: 'data:image/jpeg;base64,...',
  mimeType: 'image/jpeg',
  uploadedAt: '2026-08-10T...'
}
```

### 7.5 暂停记录（Pause）

```javascript
{
  id: 'pau_' + timestamp,
  assetId: 'ast_...',
  type: 'pause',                 // pause / transfer
  start: '2026-08-10',
  end: '2026-09-10',
  note: '',
  createdAt: '2026-08-10T...'
}
```

---

## 八、风险规则引擎

### 8.1 规则体系总览

青付安内置 **17 条风险评估规则**，覆盖预付消费全生命周期，分三个阶段执行：

| 阶段 | 规则编号 | 规则名称 | 风险等级逻辑 |
|------|---------|---------|-------------|
| **购前决策** | R1 | 单次/日均成本核算 | 对比月度预算，超标 → high |
| | R2 | 有效期-频率匹配度 | 每周所需次数 vs 计划频率差距 >1.5 → high |
| | R3 | 合同/凭证可得性核验 | 无书面合同 → medium |
| | R4 | 退款条款清晰度 | 仅口头承诺 → **high** |
| | R5 | 转卡/暂停/延期条款 | 未明确手续费 → medium |
| | R6 | 迁址/停业应对条款 | 长周期+无条款 → medium |
| | R7 | 合同主体一致性核验 | 店名≠合同≠收款方，或个人收款 → **high** |
| | R8 | 高金额预付规则 | 总价 > 3倍月预算 → **high** |
| | R9 | 赠品/限时优惠限制说明 | 有赠品但规则不清 → medium |
| **购后管理** | R10 | 到期预警 | 剩余 ≤30天 → high，≤60天 → medium |
| | R11 | 使用频率异常预警 | 实际/计划 < 0.5 → high（购前不适用） |
| | R12 | 材料留存完整性检查 | 缺失 ≥3 类 → **high**，缺失 2 类 → medium |
| | R13 | 退款前置检查 | 退款规则未知 → medium |
| **商户履约** | R14 | 服务变更/价格调整 | 默认 low（需用户主动记录） |
| | R15 | 场景专属子规则 | 默认 low |
| | R16 | 退款渠道核验 | 默认 medium（商家通常不主动告知） |
| | R17 | 平台团购支付风险 | 非直接付商家 → medium（维权链路更长） |

### 8.2 六维度评分体系

将 17 条规则按主题归入 6 个维度，每个维度独立评分（1-5 分）：

```
维度评分 = max(1, min(5, 3 + 通过数 - 高风险数×2 - 中风险数))
```

| 维度 | 包含规则 | 评估要点 |
|------|---------|---------|
| **预付压力** | R1, R2, R8 | 成本 + 频率 + 金额 |
| **履约时限** | R9, R10, R11 | 赠品期限 + 到期 + 频率异常 |
| **合约权责** | R3, R4, R5, R6 | 合同 + 退款 + 转卡 + 迁址 |
| **主体一致** | R7 | 门店/合同/收款三方一致性 |
| **证据留存** | R12, R13 | 材料 + 退款前置 |
| **促销甄别** | R14, R15, R16, R17 | 变更 + 场景 + 渠道 + 团购 |

### 8.3 综合风险等级

```
高风险项 ≥ 3          → 🔴 高风险（建议谨慎决策）
高风险项 ≥ 1 或 中风险 ≥ 4 → 🟠 中风险（有些地方需要核实）
其他                    → 🟢 低风险（看起来还不错）
```

### 8.4 无限次模式专属分析

当用户选择"充卡不限次数"模式时，规则引擎**自动切换**为充卡价值分析：

- **R1**：从"单次成本"切换到"日均/月均成本"
- **R2**：不评估频率匹配，改为追踪到店频率
- **成本测算**：展示日均成本、月均成本、回本所需每周到店次数
- **回本分析**：根据场景调整市场参考价（健身 ¥80、培训 ¥200、摄影 ¥800、美发 ¥100），计算回本所需的每周最低到店频率

---

## 九、UI/UX 设计体系

### 9.1 配色方案（已定稿）

| 用途 | 色值 | 色块 |
|------|------|------|
| **主色（薄荷绿）** | `#48A9A6` | ████████ |
| **浅底色** | `#B8E6E1` | ████████ |
| **按压加深** | `#9FD8D2` | ████████ |
| **正文色** | `#245957` | ████████ |
| **辅助文字** | `#4A7A77` / `#638F8D` | ████████ |
| **危险色** | `#E8686A` | ████████ |
| **页面背景** | `#F5FAFA` | ████████ |
| **卡片边框** | `1.5px solid #48A9A6` | — |

### 9.2 设计原则

1. **结论先行**：决策卡页面先展示综合结论横幅，再做细节展开
2. **说人话**：所有规则描述用口语化表达，避免法律术语堆砌
3. **只吵醒该吵的**：高风险项红色高亮，通过项默认折叠不扰人
4. **给台阶下**：高风险时主按钮改为"我已知晓风险，仍要生成"，返回按钮加粗引导修改
5. **SVG 全线图标**：30+ 线条图标，24×24 viewBox，颜色通过 CSS 继承，无 emoji

### 9.3 交互组件规范

| 组件 | 规范 |
|------|------|
| **输入框** | 高度 44px，圆角 12px，薄荷绿边框 |
| **主按钮** | 高度 44-50px，薄荷绿填充，按压缩放至 96% + 加深至 `#9FD8D2` |
| **次按钮** | 白底薄荷绿边框，按压时背景 `#B8E6E1` |
| **卡片** | 圆角 12-16px，薄荷绿 1.5px 边框，左侧 3px 竖条装饰 |
| **Toast** | 深色半透明底，3 秒自动关闭，位置避开 TabBar |
| **弹窗** | 343px 宽，圆角 16px，半透明黑色遮罩 |
| **进度条** | 4-6px 高，薄荷绿填充 |
| **Toggle** | 36×20px 胶囊滑块，激活时薄荷绿底 |

### 9.4 适配策略

- 移动端优先设计，适配 320-428px 宽度
- 底部 TabBar 使用 `env(safe-area-inset-bottom)` 适配刘海屏
- 所有可点击区域 ≥ 44px 满足 iOS 点击热区要求
- 页面过渡使用 0.2s 淡入 + 8px 右移

---

## 十、安全与隐私

### 10.1 数据安全

| 措施 | 说明 |
|------|------|
| **全量本地存储** | 100% 数据存储在用户设备 localStorage，无服务端传输 |
| **PIN 码锁定** | SHA-like 哈希（`btoa('qf_' + pin).slice(0, 32)`），不可逆 |
| **锁定后 UI 屏蔽** | 锁定后金额/次数显示 `•••`，按钮操作拦截 |
| **三重防护** | ①启动锁屏 ②运行时底部锁横幅 ③点横幅直通验证 |

### 10.2 PIN 锁机制

```
设置流程：用户输入6位PIN → btoa编码 → 存 qf_pin_hash
解锁流程：输入PIN → btoa编码 → 比对 qf_pin_hash → 设 qf_unlocked='1'

锁定状态：
  - 主动锁定：我的页面 → 锁定信息 → 实时生效
  - 被动锁定：清 qf_unlocked → 页面底部显示 🔒 深色胶囊横幅
  - 忘记PIN：弹窗 → 确认 → 清除 qf_pin_hash 重置
```

### 10.3 隐私承诺

- 无需注册账号
- 无需手机号验证
- 无服务端存储，无数据收集
- 换手机需手动迁移（未来可扩展导出/导入）
- 百度统计仅采集匿名页面访问数据，不关联用户身份

---

## 十一、部署与运维

### 11.1 部署方式

| 环境 | 地址 | 说明 |
|------|------|------|
| **本地开发** | `http://localhost:3000` | `npm install && npm run dev` |
| **生产环境** | `https://lin-hmpp.github.io/qingfuan/` | GitHub Pages |
| **部署方式** | push `main` → GitHub Actions 自动构建 → `gh-pages` 分支 | 全自动 |

### 11.2 兼容性

| 平台 | 兼容情况 |
|------|---------|
| iOS Safari (≥14) | ✅ 全面支持 |
| Android Chrome (≥90) | ✅ 全面支持 |
| 微信内置浏览器 (iOS) | ✅ 测试通过 |
| 微信内置浏览器 (Android) | ✅ 测试通过 |
| 桌面 Chrome/Edge | ✅ 可用但非重点优化 |

### 11.3 PWA 配置

- `manifest.json`：配置应用名称、图标、主题色
- Service Worker：缓存静态资源，支持离线访问
- 可添加至主屏幕，体验接近原生 App

---

## 十二、未来规划

### 12.1 短期迭代（V2.1）

| 优先级 | 功能 | 说明 |
|--------|------|------|
| P0 | 数据导出/导入 | 跨设备迁移，JSON 格式 |
| P1 | 消费日历视图 | 在首页添加月度核销可视化 |
| P1 | 推送提醒 | 到期前 7 天/3 天桌面通知 |
| P2 | 多维度数据统计 | 各场景消费占比、月度趋势图 |
| P2 | 暗黑模式 | 跟随系统主题自动切换 |

### 12.2 中期规划（V3.0）

| 功能 | 说明 |
|------|------|
| **云端同步（可选）** | WebDAV / GitHub Gist 备份，用户自主选择，默认关闭 |
| **AI 条款识别** | 拍照上传合同 → OCR → 自动提取退款/转卡/暂停条款 |
| **商家评分社区** | 匿名化的商户履约数据共享（需审慎评估隐私风险） |
| **多语言支持** | EN/JP 国际化 |

### 12.3 已知限制

- **存储容量**：localStorage 通常限制 5-10MB，大量 base64 图片可能超限，建议提示用户控制上传数量
- **浏览器兼容**：隐私模式下 localStorage 可能受限（iOS Safari ≤16）
- **数据持久性**：清除浏览器数据会导致所有记录丢失，建议开启定期导出提醒
- **无后端**：无法实现跨设备实时同步、社交分享等功能

---

## 附录

### A. 路由表（11个）

| 路径 | 页面 | Tab 栏 |
|------|------|--------|
| `/home` | 首页 | ✅ |
| `/quick-input` | 快速录入 | ❌ |
| `/package-input` | 套餐录入 | ❌ |
| `/decision-card` | 决策卡 | ❌ |
| `/risk-report` | 风险报告 | ❌ |
| `/asset-list` | 资产列表 | ✅ |
| `/asset-detail` | 持仓卡 | ❌ |
| `/write-off` | 核销录入 | ❌ |
| `/evidence-folder` | 证据资料夹 | ✅ |
| `/folder-create` | 新建资料夹 | ❌ |
| `/mine` | 我的 | ✅ |

### B. localStorage Key 清单

| Key | 内容 | 数据量级 |
|-----|------|---------|
| `qf_account` | 账户信息 | 1 条 |
| `qf_assets` | 资产卡片数组 | N 条 |
| `qf_writeoffs` | 核销记录数组 | M 条 |
| `qf_pauses` | 暂停记录数组 | K 条 |
| `qf_folders` | 资料夹数组 | N 条 |
| `qf_files` | 凭证文件（含 base64） | L 条 |
| `qf_draft` | 套餐录入草稿 | 0-1 条 |
| `qf_draft_time` | 草稿时间戳 | 1 条 |
| `qf_logs` | 操作日志 | 0-N 条 |
| `qf_package_images` | 录入页图片临时存储 | 0-N 条 |
| `qf_pin_hash` | PIN 哈希值 | 0-1 条 |
| `qf_unlocked` | 解锁标记 | 0-1 条 |

### C. 8 类证据材料清单

| 序号 | 分类 | 材料类型 | 关键程度 |
|------|------|---------|---------|
| ① | 交易依据 | 合同协议 | ★★★ |
| ① | 交易依据 | 付款截图 | ★★★ |
| ② | 宣传承诺 | 活动海报 | ★★☆ |
| ② | 宣传承诺 | 销售聊天记录 | ★★☆ |
| ③ | 履约记录 | 核销打卡记录 | ★★☆ |
| ④ | 问题记录 | 迁店/停业通知 | ★☆☆ |
| ④ | 问题记录 | 退费沟通记录 | ★☆☆ |
| ⑤ | 用户诉求 | 退款转卡协商材料 | ★☆☆ |

---

> **文档维护者**：青付安开发团队  
> **最后更新**：2026-08-10  
> **项目仓库**：[GitHub](https://github.com/Lin-HMPP/qingfuan)  
> **在线体验**：[https://lin-hmpp.github.io/qingfuan/](https://lin-hmpp.github.io/qingfuan/)
